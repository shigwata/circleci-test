version: 2

references:
  workspace_root: &workspace_root
    /tmp/workspace
  attach_workspace: &attach_workspace
    attach_workspace:
      at: *workspace_root
  load_code: &load_code
    run:
      name: load code from workspace
      command: |
        # Move all files and dotfiles to current directory
        mv /tmp/workspace/app/* /tmp/workspace/app/.[!.]* .

jobs:

  checkout_code:
    working_directory: ~/app
    docker:
      - image: google/cloud-sdk:178.0.0
    steps:
      - checkout
      - run:
          command: |
            mkdir -p /tmp/workspace/app
            mv * .[!.]* /tmp/workspace/app/
      - persist_to_workspace:
          root: *workspace_root
          paths:
            - app

  build:
    working_directory: /go/src/github.com/shigwata/circleci-test
    docker:
      - image: circleci/golang:1.8
    environment:
      GAE_GO_SDK_VERSION: 1.9.61
    steps:
      - *attach_workspace
      - *load_code
      - run: ls
      - run: go version
      - run: printenv

      - run:
          name: Install go_appengine
          working_directory: ~/
          command: |
            curl -o go_appengine.zip https://storage.googleapis.com/appengine-sdks/featured/go_appengine_sdk_linux_amd64-${GAE_GO_SDK_VERSION}.zip
            unzip -q -d $HOME go_appengine.zip
            echo 'export PATH=$PATH:~/go_appengine' >> $BASH_ENV
            mkdir -p /tmp/workspace/go_appengine
            cp -r ~/go_appengine/ /tmp/workspace/go_appengine/
      - run: goapp version
      - run: goapp env

      - run: |
          touch ./go/go2.txt
          ls ./go/
          mkdir -p /tmp/workspace/app/go/
          mv ./go/* /tmp/workspace/app/go/
      - persist_to_workspace:
          root: *workspace_root
          paths:
            - app/go
      - persist_to_workspace:
          root: *workspace_root
          paths:
            - go_appengine

  build_java:
    working_directory: ~/app
    docker:
      - image: circleci/openjdk:8-jdk-browsers
    steps:
      - *attach_workspace
      - *load_code
      - run: ls
      - run: java -version
      - run: mvn --version
      - run: printenv
      - run: |
          touch ./java/java2.txt
          ls ./java/
          mkdir -p /tmp/workspace/app/java/
          mv ./java/* /tmp/workspace/app/java/
      - persist_to_workspace:
          root: *workspace_root
          paths:
            - app/java

  build_node:
    working_directory: ~/app
    docker:
      - image: circleci/node:8.9.1
    steps:
      - *attach_workspace
      - *load_code
      - run: ls
      - run: npm version
      - run: printenv
      - run: |
          touch ./node/node2.txt
          ls ./node/
          mkdir -p /tmp/workspace/app/node/
          mv ./node/* /tmp/workspace/app/node/
      - persist_to_workspace:
          root: *workspace_root
          paths:
            - app/node

  deploy:
    working_directory: ~/app
    docker:
      - image: google/cloud-sdk:178.0.0
    environment:
      GAE_GO_SDK_VERSION: 1.9.61
    steps:
      - *attach_workspace
      - *load_code
      - run:
          name: load go_appengine from workspace
          working_directory: ~/
          command: |
            cp -r /tmp/workspace/go_appengine/ ~/go_appengine/
            echo 'export PATH=$PATH:~/go_appengine' >> $BASH_ENV

      - run: ls go
      - run: ls java
      - run: ls node
      - run: gcloud --version
      - run: printenv

      - run: goapp version
      - run: goapp env

      - run: echo "deploy done!"

workflows:
  version: 2
  build_and_deploy:
    jobs:
      - checkout_code
      - build:
          requires:
            - checkout_code
      - build_java:
          requires:
            - checkout_code
      - build_node:
          requires:
            - checkout_code
      - deploy:
          requires:
            - build
            - build_java
            - build_node
